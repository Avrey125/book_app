<!DOCTYPE html>
<html>
    <head></head>
    <body>
        <h1>Book LIst</h1>
        <p><a href="/">Home</a></p>
        <ul>
            <%= bookResults.forEach(item => { %>
                <li><%= item.title %></li>
            <% }) %>
        </ul>
    </body>

</html>